export interface dataGit {
    "titulo"        : string,
    "descripcion"   : string,
    "url"           : string
}

export interface dataTemplate {
    "titulo"        : string,
    "descripcion"   : string,
    "version"       : string,
}